#include <stdio.h>
/**
*main - display outcome
*Description: z to a lower
*Return: 0
*/
int main(void)
{
	char ch;

	for (ch = 'z', ch >= 'a', ch--)
	{
		putchar(ch);
	}
	putchar('\n')
	return (0);
}
