#include "holberton.h"

/**
 *print_line - print line
 *Description: print line based on n
 *@n: number
 */
void print_line(int n)
{
	char ch = '_';
	char end = '\n';

	int i

	{
		if (i > 0)
		{
			for (i = 0 ; i <= n ; i++)
				_putchar(ch);
		}
		_putchar(end);
		else if
			_putchar(end);
	}
}
